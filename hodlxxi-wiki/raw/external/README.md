# Raw External Evidence

This folder stores non-repository reference artifacts when they are required for context.

- Record provenance (source URL/system and capture date) in each artifact.
- Keep files append-only; add new versions instead of rewriting old captures.
- External evidence should never override repository/runtime evidence without explicit reconciliation notes.
