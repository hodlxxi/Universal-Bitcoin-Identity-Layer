# Raw Repository Evidence

This folder stores repository-derived evidence snapshots (route inventories, code excerpts, test output extracts).

- Keep snapshots append-only with timestamped filenames.
- Prefer plain text or JSON for diff-friendly review.
- Use these files to support `repo-defined` and `test-verified` synthesis claims.
